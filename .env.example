# คัดลอกไฟล์นี้เป็น .env แล้วแก้ค่าให้ตรงกับเครื่องคุณ
# cp .env.example .env

# การเชื่อมต่อฐานข้อมูล PostgreSQL (ใช้เฉพาะฝั่ง server functions)
# ถ้าไม่ตั้งค่านี้ Dashboard จะแสดงข้อมูล mock แทน (เหมาะกับการพัฒนา UI ก่อน)
DATABASE_URL=postgres://user:password@localhost:5432/jobs_analysis
